<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.sickkids.nopainapp"
    android:versionCode="1"
    android:versionName="1.0" >
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
	<uses-permission android:name="android.permission.RECORD_AUDIO" />    
    <uses-permission android:name="android.permission.CAMERA" />  
	
    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="ca.sickkids.nopainapp.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ca.sickkids.nopainapp.Journaling" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.HomeActivity" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.SurveyActivity" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.SocialNetworkActivity" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.VideoActivity" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.Recommendation" android:label="@string/app_name"></activity>
        <activity android:name="ca.sickkids.nopainapp.CalendarActivity" android:label="@string/app_name"></activity>
        <!-- <activity android:name=".ViewVideo"> -->
        <!--          <intent-filter> -->
        <!--               <action android:name="android.intent.action.VIEW" /> -->
        <!--                <category android:name="android.intent.category.DEFAULT" /> -->
        <!--        </intent-filter> -->
        <!-- </activity> -->
    </application>

</manifest>